apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: web-fintrack-cert
  namespace: fintrack
spec:
  dnsNames:
    - failtrack.kbaenak.tech
  secretName: app-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
