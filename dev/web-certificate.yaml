apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: web-fintrack-cert
  namespace: fintrack
spec:
  dnsNames:
    - fintrack.kbaenak.tech
  secretName: fintrack-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
