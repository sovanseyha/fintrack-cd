apiVersion: apps/v1
kind: Deployment
metadata:
  name: fintrack-ui
  namespace: fintrack
  labels:
    app: fintrack-ui
spec:
  replicas: 4
  selector:
    matchLabels:
      app: fintrack-ui
  template:
    metadata:
      labels:
        app: fintrack-ui
    spec:
      containers:
      - name: fintrack-ui
        image: kimheang68/ui-fintrack:16
        ports:
        - containerPort: 80
      
